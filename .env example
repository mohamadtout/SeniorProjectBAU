SERVER_PORT = 3000 (optional/ defaults to 3000)
NODE_ENV = DEVELOPMENT
JWT_SERCRET = (shared secretly)
API_URL = http://127.0.0.1 (this should be changed to http://10.0.2.2 if testing for android on emulator)